# Git & Commit Rules

## Strict Policy: No Autonomous Commits
- **DO NOT** run `git commit` or `git push` automatically.
- Never commit on behalf of user unless user explicitly gives command (e.g. `/commit`, "commit", "push").
- User manually stages, commits, or instructs when ready.
